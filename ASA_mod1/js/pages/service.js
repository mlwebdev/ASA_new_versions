// function serviceForm(serviceForm, serviceSelect) {
//   serviceForm = f.form_,
//   serviceSelect = f.create("select");
// }
/*
(function (serviceSelect, service, makeCheckBoxes, optionsText) {
  serviceSelect = getId("serviceSelect");
  services = [ 
    "--Select Services--", "Oil change", "Tune up", "Air filter change", "Fuel filter change", 
		"Transmission flush", "Radiator flush", "Tire rotation"
  ];
  for(let service of services){
    makeCheckBoxes = document.createElement("option"); 
    optionsText = service;						
    makeCheckBoxes.setAttribute("value", optionsText);
    makeCheckBoxes.innerHTML = optionsText;
    serviceSelect.appendChild(makeCheckBoxes);
  } // End for - of
})();
*/